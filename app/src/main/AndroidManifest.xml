<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kcs.com.logicart">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_kcs"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_kcs"
        android:supportsRtl="true"
        android:theme="@style/MyOwntheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MenuActivity"/>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".ForgotPassActivity"/>
        <activity android:name=".DaftarHargaActivity"/>
        <activity android:name=".TransaksiHarianActivity"/>
        <activity android:name=".PanduanTopupActivity"/>
    </application>

</manifest>